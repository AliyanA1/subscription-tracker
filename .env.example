# -----------------------------
# 🔧 Basic Configuration
# -----------------------------
PORT=                 # Port number your app runs on
SERVER_URL=           # Base URL of the server (e.g., http://localhost:3000)
NODE_ENV=             # Environment: 'development' | 'production'

# -----------------------------
# 🛢️ Database Configuration
# -----------------------------
DB_URI=               # MongoDB connection string

# -----------------------------
# 🔐 JWT Configuration
# -----------------------------
JWT_SECRET=           # Secret key for signing JWT tokens

# -----------------------------
# 🛡️ Arcjet Integration
# Documentation: https://arcjet.com/
# -----------------------------
ARCJET_KEY=           # Arcjet API key (from dashboard)
ARCJET_ENV=           # Environment name (e.g., 'development', 'production')

# -----------------------------
# 📬 QStash (Email Reminder)
# Documentation: https://upstash.com/
# -----------------------------
QSTASH_URL=                   # QStash endpoint URL
QSTASH_TOKEN=                 # API token for authentication
QSTASH_CURRENT_SIGNING_KEY=  # Current key used for verifying requests
QSTASH_NEXT_SIGNING_KEY=     # Next key used for key rotation

# 💡 To run QStash locally:
#    Run this in a separate terminal:
#    npx @upstash/qstash-cli dev
#    This will start the local environment for QStash.

# -----------------------------
# 📧 Nodemailer Configuration
# -----------------------------
ACCOUNT_EMAIL=        # Your email address (used to send emails)
EMAIL_PASSWORD=       # App-specific password or secret from Google

# -----------------------------
# 📁 Environment Files Required
# -----------------------------
# You must create the following environment files:
# - .env.development.local  -> for development mode
# - .env.production.local   -> for production mode
#
# In `.env.production.local`, explicitly set:
# NODE_ENV=production
